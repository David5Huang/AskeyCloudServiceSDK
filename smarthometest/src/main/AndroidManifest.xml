<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.askey.qbee.client">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">


        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MasterTestActivity"/>
        <activity android:name=".activity.CognitoMqttTestActivity"/>
        <activity android:name=".activity.AuthTestActivity"/>
        <activity android:name=".activity.ThingTestActivity"/>

        <service
            android:name="tw.com.askey.webservice.sdk.iot.MqttService"
            android:exported="true"
            />

    </application>

</manifest>
